import { Stack } from "@mui/material";
import styled from "@mui/material/styled";

export const MainAreaStyles = styled(Stack)(() => ({
  borderRadius: "15px",
  height: "100px",
  width: "100px",
}));
